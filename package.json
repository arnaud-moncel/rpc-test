{
  "name": "rpc-test",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "start": "nodemon index.ts",
    "seed": "ts-node ./scripts/seed.ts",
    "install:all": "yarn install && for d in group-agent user-agent projet-agent; do (cd \"$d\" && yarn install); done"
  },
  "nodemonConfig": {
    "ignore": [
      "typings.ts",
      ".forestadmin-schema.json"
    ]
  },
  "dependencies": {
    "@forestadmin-experimental/datasource-rpc": "^1.13.0",
    "@forestadmin/agent": "^1.79.4",
    "dotenv": "^16.4.7",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.6.0",
    "knex": "^3.1.0",
    "pg": "^8.13.1"
  }
}
